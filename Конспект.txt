pip install fastapi
pip install uvicorn[standard]
pip install email-validator
pip install jinja2
pip install python-multipart # По умолч. FastAPI обрабатывает только JSON
pip install isort



uvicorn main:app --reload
Можно указать:
--port 8000

mypy <название_файла>
